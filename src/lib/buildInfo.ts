/**
 * Bump this string every time a new zip is shipped. It shows in the
 * sidebar footer so it's possible to tell, at a glance from a screenshot,
 * whether a deployment actually picked up the latest code — instead of
 * guessing from which features are visible.
 */
export const BUILD_ID = "2026-08-31.3";
